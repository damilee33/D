# 2026年华为杯D题题目分析报告

## 1 任务边界与当前状态

本报告面向题目《山区洪涝灾害下无人机运输与通信协同优化》，用于冻结后续代码实现前的模型合同。分析以题目正文、原始附件和用户确认的 `D题_Codex可执行开发规范.md` 为依据；附件保持只读。

当前采用临时能耗口径 `provisional_v0`。该口径仅用于在官方能耗定义缺失期间推进建模与程序结构验证，不代表官方公式。官方发布新定义后，必须替换公共能耗模块、废止受影响缓存，并从 Q1 开始重算 Q1—Q4；旧结果不得继续作为正式结论。

2026 年竞赛于 9 月 23 日 08:00 开赛，作品提交窗口为 9 月 27—28 日，截止 9 月 28 日 24:00。最终提交格式、命名和平台要求仍以当届开赛公告及系统页面为准。

## 2 题目与附件清单

### 2.1 核心输入

| 输入 | 内容 | 已核验事实 | 使用方式 |
|---|---|---|---|
| `山区洪涝灾害下无人机运输与通信协同优化.docx` | Q1—Q4、附录 1—3、公式和参考文献 | 113 个非空/公式段落、1 幅内嵌地图 | 最高优先级题面，只读 |
| `D题_Codex可执行开发规范.md` | 公共物理层、阶段继承、baseline 与主模型 | 与题目结构一致；临时能耗公式由用户另行授权 | 软件与模型路线合同 |
| `调度中心与服务区.xlsx` | O01 与 S001—S015 的经纬度、海拔、人口 | 1 个中心、15 个唯一服务区 | 节点与作业海拔 |
| `物资需求与配送时限.xlsx` | 汇总需求及逐箱明细 | 80 个唯一货箱、15 个服务区 | 需求、时限与优先级 |
| `运输无人机数据.xlsx` | A/B/C 机型、8 架实体机、电池池 | 4A、2B、2C；电池 6/4/4 组 | 运输能力与资源 |
| `中继无人机数据.xlsx` | 中继机、能源组件、功率与时间参数 | 2 架中继机、6 组能源组件 | 中继调度与能耗 |
| `通信链路参数.xlsx` | 频率、损耗、收发功率和天线参数 | 2400 MHz，系统损耗 3 dB，遮挡附加损耗 10 dB | 双向链路预算 |
| `镇龙乡及周边30米DEM.tif` | 地形高程 | 1486×1309，WGS 84 / EPSG:4326，高程约 41.69—1132.86 m | 航段净空与 LOS |
| `镇龙乡地理空间数据说明.pdf` | DEM、道路、水体、水系、村镇说明 | 名义分辨率 30 m，NoData 口径 −32767 | 坐标与数据口径 |
| `结果提交模板.xlsx` | Q1—Q4 结果工作表结构 | 6 个工作表，当前仅含字段模板 | 最终阶段填充，不作为唯一结果源 |

题面写有“地理空间数据说明.docx”，实际提供的是 PDF；这是附件文件名差异，不改变数据口径。道路、水系、水体和村镇点位仅用于场景核验和可视化，不增加题面未规定的禁飞约束。

### 2.2 数据审计结论

- 逐箱清单共 80 箱，货箱编号全部唯一；医疗 16 箱、饮用水 36 箱、应急食品 19 箱、生活卫生用品 9 箱。
- 总质量为 758 kg，总体积为 2.011 m³，首批保障货箱为 30 箱。
- 汇总需求与逐箱清单在箱数、质量、体积和首批箱数上逐项一致。
- 非首批货箱的首批截止时间均为空，属于结构性空值；首批货箱截止时间均非空。
- O01 和 15 个服务区经纬度均落入 DEM 说明给出的覆盖范围。
- 所有运输机、中继机、电池和能源组件编号及机型映射完整，未发现重复 ID。
- GeoTIFF 内当前没有实际值等于 −32767 的像元，但仍按元数据口径保留 NoData 检查。

## 3 四个子问题的结论目标

### 3.1 Q1 单点往返能力与组批

输出 15×3 的最大安全载荷矩阵；为每个服务区给出不可拆货箱的批次、机型、质量、体积、时间、能耗和返航 SOC；比较 baseline 与主模型，并给出 10%—30% 安全余量敏感性。最终结论必须说明架次数、总能耗和累计作业时间之间的 Pareto 权衡，不能只给综合评分。

**答题者定义：**题面未规定“累计作业时间”的唯一口径。本方案将其定义为所有 Q1 架次的准备、逐箱装载、完整往返飞行、基础交接和逐箱交接时间之和。Q1 不分配实体无人机，故这里采用各架次时间相加，不采用并行排程后的完成时间；运输机附件未提供周转时间，Q1 不自行增加周转项。该定义属于模型设计，应在论文中与数值结果同时说明。

### 3.2 Q2 异构多点多架次调度

输出全部货箱唯一交付的可重放运输方案，包括路线、访问顺序、实体无人机、电池、开始时刻、逐段载荷、逐箱交付时刻、SOC 与充电区间。在硬时限和资源约束零违例的前提下，比较及时性损失、完成时间、能耗和架次数。

### 3.3 Q3 运输与通信中继联合调度

以通过验收的 Q2 方案为父版本，输出运输轨迹的直连/中继状态、通信缺口、中继悬停点与高度、中继时段、能源组件和两段链路证据。目标是全过程通信中断时长为零，并给出联合完成时间、总能耗和两类架次数的 Pareto 方案。

### 3.4 Q4 分区与独立资源配置

固定 Q3 的组批、访问顺序、运输与中继关系，分别给出 K=2 和 K=3 的服务区分组；核算每组独立执行所需的分机型运输机、电池、中继机和能源组件，并说明工作量均衡、资源冗余及相对库存缺口。

## 4 全局模型合同

| 合同项 | 冻结内容 |
|---|---|
| 任务定义 | 构建公共物理层，并依次求解 Q1 单点组批、Q2 异构运输、Q3 通信中继、Q4 任务分区 |
| 数据 | 全部原始附件只读；内部统一使用 m、s、kg、m³、kWh、0—1 SOC、MHz、km、dB/dBm |
| 结论类型 | 可重放调度、Pareto 指标、约束审计、灵敏度/稳定性和资源缺口，不输出未经运行的预测数值 |
| 模型 | Q1 多约束 Bin Packing；Q2 异构多仓次 VRP 与资源调度；Q3 时空覆盖与中继调度；Q4 must-link 约束图划分 |
| 数学定义 | 全部物理量由唯一公共模块计算；Q2 继承 Q1 经验但不锁死批次，Q3 继承并可局部调整 Q2，Q4 只读冻结的 Q3 |
| 实现 | Python；公共函数、版本化 JSON/CSV、独立重放器；优化器不得直接解释原始 Excel |
| 验证 | baseline、边界单元测试、独立重放、约束零违例、Pareto 非支配、多种子/网格加密/参数敏感性 |
| 风险 | 能耗官方口径更新、DEM 穿栅格或 LOS 漏检、Q2 无可行解、Q3 覆盖不足、Q4 must-link 结构性不可分 |

## 5 公共物理计算层

### 5.1 坐标、距离与 DEM

节点使用 WGS 84 经纬度。短距离水平距离采用可靠的椭球测地线或等价的局部投影计算，不直接把经纬度差当作米。对有向航段 $i\rightarrow j$，沿直线执行栅格穿越，遍历相交 DEM 像元，得到水平距离 $d_{ij}$、最高地面高程 $H^{\max}_{ij}$ 和巡航海拔

$$
H^{\mathrm{cruise}}_{ij}=H^{\max}_{ij}+50.
$$

O01 作业海拔等于地面海拔，服务区作业海拔等于地面海拔加 30 m。爬升和下降高度分别为

$$
h^+_{ij}=\max\{0,H^{\mathrm{cruise}}_{ij}-H_i^{\mathrm{work}}\},\qquad
h^-_{ij}=\max\{0,H^{\mathrm{cruise}}_{ij}-H_j^{\mathrm{work}}\}.
$$

水平距离和最高地形对正反向一致，爬升与下降可以不同。DEM 边界、NoData 和所有节点覆盖必须在开工门禁中再次断言。

### 5.2 飞行时间

运输机型 $g$ 在航段 $i\rightarrow j$ 的飞行时间为

$$
t_{gij}=\frac{h^+_{ij}}{v_g^{\uparrow}}+\frac{d_{ij}}{v_g^{c}}+\frac{h^-_{ij}}{v_g^{\downarrow}}.
$$

多点访问时，每次投送后均从该服务区地面以上 30 m 的作业高度重新爬升。准备、逐箱装载、基础交接、逐箱交接和周转时间进入统一时间线，不与飞行时间混为一项。

### 5.3 临时运输能耗模型 `provisional_v0`

题面给出的等效航程为

$$
L_g(q)=L_g^0-\left(L_g^0-L_g^F\right)\left(\frac{q}{Q_g}\right)^{3/2},qquad 0\le q\le Q_g.
$$

经用户授权，暂采用以下水平巡航能耗：

$$
E^{\mathrm{hor}}_{gij}(q)=\frac{d_{ij}}{L_g(q)}E_g^{\mathrm{use}}.
$$

爬升附加能耗暂采用重力势能除以效率：

$$
E^{\mathrm{up}}_{gij}(q)=
\frac{(m_g+q)g_0h^+_{ij}}{\eta_g\,3.6\times10^6},
$$

其中 $m_g$ 为含电池空载总质量，$g_0=9.80665\ \mathrm{m/s^2}$，$\eta_g$ 为附件中的爬升能耗效率。总航段能耗为

$$
E_{gij}(q)=E^{\mathrm{hor}}_{gij}(q)+E^{\mathrm{up}}_{gij}(q).
$$

下降附加能耗按题面取 0。多点架次在每个航段使用卸货后的实际剩余载荷。架次 $p$ 必须满足

$$
E_p^T=\sum_{(i,j)\in p}E_{gij}(q_{pij})\le (1-\rho_g)E_g^{\mathrm{use}}.
$$

版本化要求：能耗函数、参数和测试集中于公共模块；所有 S1/S2/S3、缓存和复现清单记录 `energy_model_version=provisional_v0` 与公式配置哈希。官方更新后，不允许仅修改论文。

### 5.4 临时中继能耗模型 `provisional_v0`

中继机巡航功率为 $P_R^c$，悬停功率为 $P_R^h$，通信附加功率为 $P_R^{\mathrm{com}}$。若相应时间以秒计，则

$$
E_R^{\mathrm{cruise}}=\frac{P_R^c t_R^c}{3600},\qquad
E_R^{\mathrm{hover}}=\frac{(P_R^h+P_R^{\mathrm{com}})t_R^h}{3600}.
$$

爬升能耗使用计划起飞总质量 $m_R$：

$$
E_R^{\mathrm{up}}=\frac{m_Rg_0h_R^+}{\eta_R\,3.6\times10^6}.
$$

中继架次总能耗由出航、悬停服务和返航阶段求和，并满足 20% 返航电量下限。该公式与运输能耗一同受官方更新门禁控制。

### 5.5 SOC 与两阶段充电

对每块电池/能源组件维护独立 ID、任务区间、任务后 SOC、充电区间和再次可用时刻。任务后 SOC 为

$$
s_{\mathrm{after}}=s_{\mathrm{before}}-\frac{E_{\mathrm{mission}}}{E^{\mathrm{use}}}.
$$

再次执行任务前必须恢复至 100%。题面两阶段充电函数为

$$
t_{\mathrm{chg}}(s)=
\begin{cases}
T_{\mathrm{full}}\left[0.65\dfrac{0.90-s}{0.90}+0.35\right],&0\le s<0.90,\\
T_{\mathrm{full}}\,0.35\dfrac{1-s}{0.10},&0.90\le s\le1.
\end{cases}
$$

必须测试 $s=0$、$0.9$、$1$ 及跨越 0.9 的情况。

### 5.6 通信链路

同一时刻端点 $i,j$ 的双向链路门限为

$$
L_{\max}^{i\leftrightarrow j}=\min\{L_{\max}^{i\rightarrow j},L_{\max}^{j\rightarrow i}\}.
$$

自由空间传播损耗和含遮挡的总损耗分别为

$$
L_{\mathrm{FSPL},ij}(t)=32.45+20\log_{10}f+20\log_{10}D_{ij}(t),
$$

$$
L_{\mathrm{path},ij}(t)=L_{\mathrm{FSPL},ij}(t)+L_{\mathrm{obs}}b_{ij}(t),
$$

其中 $f$ 用 MHz、三维距离 $D_{ij}(t)$ 用 km。直连不可用时，运输机—中继机和中继机—G01 两段链路必须在同一时刻同时可用；不允许中继多跳。

## 6 Q1 模型与算法

### 6.1 模型体系

Q1 采用一个模型体系：最大安全载荷计算与多约束 Bin Packing 串联。FFD 仅作为同一问题的 baseline，局部搜索是主模型的求解算法，不另计独立模型。鉴于单个服务区最多只有 15 个货箱，另以位掩码动态规划计算最小架次数下的能耗—累计作业时间 Pareto 前沿，**只作为最优性证书**，不替代主算法。

对服务区 $i$、机型 $g$，最大安全载荷定义为

$$
W^{\mathrm{safe}}_{ig}=\max\left\{q:\ 0\le q\le Q_g,
E_{g,O01,i}(q)+E_{g,i,O01}(0)\le(1-\rho_g)E_g^{\mathrm{use}}\right\}.
$$

先验证可行性对 $q$ 的单调性；通过后使用有界二分或单调搜索，否则改用完整区间扫描。实际组批还需满足不可拆箱、同区、质量、体积和能量约束。

对 Q1 架次 $p$，设所用机型为 $g(p)$、装载货箱数为 $n_p$，则答题者定义的单架次作业时间为

$$
T_p^{\mathrm{work}}=t_{g(p)}^{\mathrm{prep}}
+n_p t_{g(p)}^{\mathrm{load}}
+\sum_{(i,j)\in p}t_{g(p)ij}
+t_{g(p)}^{\mathrm{handover},0}
+n_p t_{g(p)}^{\mathrm{handover},\mathrm{box}}.
$$

Q1 累计作业时间为

$$
T_{\mathrm{Q1}}^{\mathrm{cum}}=\sum_{p\in\mathcal P_{\mathrm{Q1}}}T_p^{\mathrm{work}},
$$

单位为秒。该指标衡量全部架次投入的总作业量，与 Q2 的并行运输完成时间 $T_{\mathrm{transport}}$ 含义不同。

### 6.2 baseline 与主模型

- baseline：同一服务区内按重量递减，选择满足安全航程且容量足够的最小合适机型，执行 FFD。
- 主模型：从 FFD 可行批次出发，对两个批次的并集执行精确重分配，从而同时覆盖箱间移动、交换、批次合并和机型替换；当双批邻域无改进时，再搜索三批重分配邻域。每次变换均调用统一物理层和约束审计器复核。
- 最优性证书：对各服务区的货箱子集生成全部可行“子集—机型”组合，用位掩码动态规划和 Pareto 剪枝求解最小架次数前沿；若局部搜索架次数与证书不一致，则结果不得进入后续问题。
- 比较顺序：先硬约束零违例，再最小架次数，再比较总能耗与累计作业时间的非支配关系；不使用无依据加权和。

### 6.3 验证与候选图表

验证包括载荷边界两侧可行/不可行复算、货箱唯一性、同区约束、批次体积与质量、10%—30% 安全余量单调性、baseline 对比，以及局部搜索与精确证书的架次数差距检查。

候选图表：服务区—机型安全载荷热图、各区批次数/货量图、批次质量—体积双约束图、安全余量敏感性曲线、baseline 与主模型 Pareto 图。

## 7 Q2 模型与算法

### 7.1 模型体系

Q2 采用一个异构多仓次车辆路径与资源调度模型族。EDF 贪心是 baseline，分层构造加 ALNS 是主求解器；显式无人机/电池排程和独立重放属于同一模型的解码与验证部分。

决策同时包含货箱分组、服务区访问顺序、机型、实体无人机、电池、开始时刻和逐箱交付时刻。硬约束包括：货箱唯一交付、容量、能量、返航余量、路线回仓、无人机/电池兼容、资源区间不重叠、再次使用前充满、全部医疗物资不晚于其期望送达时间、首批货箱不晚于首批截止时间。

非硬时限物资的及时性损失定义为

$$
D=\sum_{c\in\mathcal C_{\mathrm{soft}}}w_c\max\{0,T_c-\tau_c\},
$$

其中 $w_c$ 为应急优先系数，$T_c$ 为交付完成时刻，$\tau_c$ 为期望送达时刻。

### 7.2 baseline 与主模型

- baseline：Q1 单区批次、单区往返、按硬截止与期望时间 EDF 排序，分配最早可用实体机和已充满的同型电池。
- 主模型：由 EDF 可行解或逐步开放多点合并的构造解起步；ALNS 使用服务区移除、最迟货箱移除、高能耗架次移除、相关区域成组移除、拆分/合并、机型替换和资源重排算子。
- 不可行邻域必须修复或拒绝；不以巨大罚值长期保留明显硬约束违例。
- 评价采用层级 Pareto：可行性 → 及时性损失 → 完成时间/能耗/架次数 → 稳健性与可解释性。

### 7.3 验证与候选图表

验证包括逐箱、逐架次、逐无人机、逐电池四视角对账；独立时间线重放；多随机种子最好值、典型值和离散程度；与 baseline 的非支配比较。

候选图表：运输路线地图、无人机甘特图、电池 SOC/充电图、逐区送达时刻与时限图、ALNS 收敛/Pareto 图、不同种子稳定性图。

## 8 Q3 模型与算法

### 8.1 模型体系

Q3 采用“通信轨迹离散与 LOS/链路预算 + 时空集合覆盖与中继资源调度”串联模型。候选点离散化是数值实现，不是另一套物理模型。

先从冻结的 S2 生成运输机全过程三维轨迹，提取 G01 直连不可用的时空缺口。候选中继点位于 DEM 有效范围，高度不超过 300 m 离地上限。覆盖变量只有在同一时刻接入链路和回传链路均可用时才为 1。

**答题者定义：**通信连续性采用可复现的逐级加密数值认证。全局时间步长依次取 5、2.5、1.25、0.625 s；候选点水平间距依次取 120、60、30 m；候选离地高度间距依次取 25、12.5、6.25 m，候选高度为正整数倍间距且不超过 300 m。每一级都重新计算 LOS、双向链路和覆盖矩阵，并从上一级方案热启动重新优化。

连续两级同时满足以下条件才判为稳定：全部通信与资源硬约束零违例；中继架次数及缺口—中继任务分配不变；及时性损失、联合完成时间和总能耗的对称相对变化均不超过 0.5%；所有被采用的中继接入链路与中继回传链路的最小预算余量不低于 1 dB。直连链路按题面官方门限判定，即预算余量不低于 0 dB；不额外要求直连达到 1 dB。对相邻两级非负指标值 $a,b$，对称相对变化定义为

$$
r(a,b)=
\begin{cases}
0,&a=b=0,\\
\dfrac{2|a-b|}{a+b},&a+b>0.
\end{cases}
$$

因此两级均为 0 时变化记为 0，只有一侧为 0 时变化为 2，判为不稳定。任一加密层发现新的通信中断，上一层方案立即判定失败并在当前精度重新优化。

若达到 30 m 水平间距、6.25 m 高度间距和 0.625 s 时间步长后仍不稳定，则仅在最小链路余量时段执行最多两级局部加密，时间步长依次减至 0.3125、0.15625 s，高度间距依次减至 3.125、1.5625 m。两级后仍不满足稳定条件时，本阶段返回 `BLOCKED`，不得宣称实现全过程连续通信。1 dB 余量仅是答题者用于数值认证的保守门槛，不改变题面以 0 dB 余量判定链路可用的硬规则；题面可用但余量不足 1 dB 的链路进入局部加密，而不是直接判为题面不可用。

### 8.2 baseline 与主模型

- baseline：固定 S2，按缺口时间顺序选择最近可行候选点、最早可用中继机和能源组件；失败后只允许调整相关运输架次开始时刻。
- 主模型：以时空集合覆盖决定缺口—中继任务匹配，以资源调度决定中继机、能源组件、架次和充电；必要时做受控局部运输调整并重新执行 Q2 全部审计。
- 不允许中继多跳；直连可用区间不占用中继资源。

### 8.3 验证与候选图表

验证包括通信中断总时长为零、两段链路同步可用、候选点高度和 DEM 边界、资源区间不冲突、上述固定序列的通信步长与候选网格加密复验，以及局部运输调整后的 Q2 约束复查。离散加密是数值认证而非解析证明；只有满足明确稳定判据和链路余量要求时才能在论文中表述为“在规定加密精度下未检出通信中断”。

候选图表：直连盲区地图、中继候选覆盖图、链路损耗—门限时间序列、中继甘特图、能源组件 SOC 图、步长/网格敏感性图。

## 9 Q4 模型与算法

### 9.1 模型体系

Q4 采用一个 must-link 约束图划分与独立资源核算模型。地理聚类是 baseline；在压缩 must-link 连通分量后使用枚举/剪枝或 0—1 整数规划求 K=2、K=3 主方案。

图节点为 15 个服务区。共同出现在同一运输架次的服务区必须同组；不可拆分中继任务造成的共同通信关系按题面口径加入硬连接。服务区完整互斥覆盖、每组非空，Q3 的组批、路径和通信关系不得改变。

### 9.2 资源核算

各组资源需求根据冻结任务时间线的最大兼容并发及充电周转求最小可行数量，不能简单统计曾出现过的资源 ID。每一组独立重放；资源不得跨组借用。

**答题者定义：**任务组 $k$ 的工作量不压缩成主观加权总分，而表示为四维向量

$$
\mathbf W_k=\left(M_k,H_k^T,H_k^R,E_k\right),
$$

其中 $M_k$ 为交付货物总质量，$H_k^T$ 为该组全部运输无人机任务占用时长之和，$H_k^R$ 为全部中继无人机任务占用时长之和，$E_k=E_{T,k}+E_{R,k}$ 为运输与中继总能耗。四个维度的单位分别为 kg、s、s、kWh。

对任一工作量维度 $\ell$，组间均衡度使用变异系数

$$
\mathrm{CV}_{\ell}=\frac{\sqrt{K^{-1}\sum_{k=1}^{K}\left(W_{k\ell}-\overline W_{\ell}\right)^2}}{\overline W_{\ell}}.
$$

由于各工作量分量均非负，当 $\overline W_{\ell}=0$ 时该维度必为全零，规定 $\mathrm{CV}_{\ell}=0$。四个变异系数组成均衡向量并参与 Pareto 比较，不进行主观加权。若必须从同一资源缺口层级的非支配方案中选出单一折中方案，则最小化 $\max_{\ell}\mathrm{CV}_{\ell}$。

设资源类型 $r$ 的现有库存为 $N_r^{\mathrm{stock}}$，任务组 $k$ 独立执行冻结 S3 所需的最小数量为 $N_{kr}^{\mathrm{req}}$。按 A/B/C 运输机、A/B/C 电池、中继机和中继能源组件分别定义总库存冗余与缺口：

$$
R_r=\max\left\{N_r^{\mathrm{stock}}-\sum_{k=1}^{K}N_{kr}^{\mathrm{req}},0\right\},
$$

$$
G_r=\max\left\{\sum_{k=1}^{K}N_{kr}^{\mathrm{req}}-N_r^{\mathrm{stock}},0\right\}.
$$

总配置规模保留为逐资源需求向量

$$
\mathbf N^{\mathrm{req}}=\left(\sum_kN_{k,A_U}^{\mathrm{req}},\sum_kN_{k,B_U}^{\mathrm{req}},\sum_kN_{k,C_U}^{\mathrm{req}},\sum_kN_{k,A_B}^{\mathrm{req}},\sum_kN_{k,B_B}^{\mathrm{req}},\sum_kN_{k,C_B}^{\mathrm{req}},\sum_kN_{k,R_U}^{\mathrm{req}},\sum_kN_{k,R_B}^{\mathrm{req}}\right),
$$

依次对应 A/B/C 型运输机、A/B/C 型电池、中继机和中继能源组件。不同资源类型不直接相加，只做向量的分量级 Pareto 比较。

Q4 的比较层级为：固定关系与独立重放可行 → 最小化逐类资源缺口向量 → 比较八维配置向量和四维均衡向量的 Pareto 关系 → 在必须单选时最小化最大变异系数。若最小最大变异系数仍并列且资源缺口向量互不可比，经答题者确认，仅为满足结果模板的唯一展示要求，优先展示发生缺口的资源类别数较少者，再按各组服务区编号序列的字典序选择；该展示规则不把不同资源数量直接相加，也不表示被展示方案在 Pareto 意义上优于其他非支配方案。baseline 不得破坏 must-link。

### 9.3 验证与候选图表

验证包括 15 个服务区完整互斥覆盖、每组非空、全部硬边未切断、逐组独立重放、分机型资源缺口复算，以及从冻结时间线独立重算 $M_k$、$H_k^T$、$H_k^R$、$E_k$ 和各维度变异系数。

候选图表：任务关联图、K=2/K=3 地图、组间工作量雷达/条形图、资源需求与库存对比图、组内任务甘特图。

## 10 假设及放宽复验

### 10.1 关键假设

1. **关键：临时能耗模型可用于阶段性计算。** 依据是用户明确授权；若官方给出不同公式，当前所有能耗相关结果失效，必须从 Q1 重跑。
2. **关键：题面直线航段和 DEM 最高像元规则完整描述地形净空。** 若官方增加禁飞区、风场或航迹绕行规则，公共航段表及全部下游方案失效。
3. **关键：通信状态由题面给定的确定性双向链路预算与 DEM 遮挡判定。** 若官方增加概率衰落、干扰或容量约束，Q3 覆盖矩阵和调度模型必须重建。

### 10.2 可放宽假设

1. **可放宽：气象与风速未给出，暂按静态无风环境。** 复验方式：对等效航程或巡航能耗乘数做情景扫描。
2. **可放宽：准备、装载、交接、建链和周转时间为确定值。** 复验方式：对关键作业时间做 ±10%/±20% 扰动。
3. **可放宽：运输航段内速度取附件计划速度。** 复验方式：官方允许变速后增加速度情景，不改变数据接口。
4. **可放宽：候选中继点与通信时间采用离散网格。** 复验方式：空间网格和时间步长逐级加密，直到结论稳定。
5. **可放宽：所有实体资源在时刻 0 位于 O01 且 SOC=100%。** 依据题面；若赛前准备状态更新，替换初始状态并重放。
6. **可放宽：道路、水体、水系不构成飞行禁区。** 依据地理说明与题面用途；若官方增加限制，转为航段可行性掩膜。

## 11 实现边界、失败条件与回退

- 数据断言不一致：输出差异并停止，不修改原始附件。
- 最大安全载荷不单调：放弃二分，改为完整区间扫描。
- Q2 ALNS 无可行解：回退 EDF baseline，从单区往返逐步开放多点合并，不放宽硬约束。
- Q3 固定 S2 覆盖失败：先调整相关开始时刻，再局部重排；仍失败则报告覆盖、并发或能源瓶颈。
- Q4 must-link 连通分量少于 K：报告结构性不可行，不拆硬连接。
- 优化器输出无法被独立重放：阶段失败，不能冻结 S1/S2/S3。
- 官方能耗公式更新：更改公共函数与版本，运行边界测试，从 Q1 全量重算并更新所有结果和论文证据。

## 12 编程依赖与复现计划

计划使用 Python，主要依赖为 NumPy、pandas、openpyxl、地理栅格/坐标库、优化建模或启发式实现库、matplotlib。环境必须在 P0 开始时实际探测；缺失依赖不得静默换算法。正式运行固定随机种子并记录输入 SHA-256、公式版本、配置哈希、依赖版本、运行时间和唯一复现命令。

代码输出按 `results/00_audit`、`results/01_common`、`results/q1`—`results/q4`、`results/validation` 分层；S1/S2/S3 含父版本、输入哈希、配置哈希、随机种子、求解状态、目标指标和硬约束摘要。

## 13 文献依据

1. Ropke, S., & Pisinger, D. (2006). *An Adaptive Large Neighborhood Search Heuristic for the Pickup and Delivery Problem with Time Windows*. Transportation Science, 40(4), 455–472. DOI: https://doi.org/10.1287/trsc.1050.0135 。用于 ALNS 的多移除/插入算子与自适应选择框架。
2. Dorling, K., Heinrichs, J., Messier, G. G., & Magierowski, S. (2017). *Vehicle Routing Problems for Drone Delivery*. IEEE Transactions on Systems, Man, and Cybernetics: Systems, 47(1), 70–85. DOI: https://doi.org/10.1109/TSMC.2016.2582745 。用于载荷—能耗相关无人机路径问题的背景依据。
3. Zhang, J., Campbell, J. F., Sweeney, D. C., & Hupman, A. C. (2021). *Energy Consumption Models for Delivery Drones: A Comparison and Assessment*. Transportation Research Part D, 90, 102668. DOI: https://doi.org/10.1016/j.trd.2020.102668 。仅用于说明能耗模型选择敏感；正式计算仍以题面和用户确认口径为准。
4. ITU-R. (2024). *Recommendation P.525-5: Calculation of Free-Space Attenuation*. https://www.itu.int/rec/R-REC-P.525-5-202411-I/en 。用于自由空间传播损耗公式的标准来源。
5. Zhang, X. et al. (2020). *Resource allocation and location decision of a UAV-relay for reliable emergency indoor communication*. Computer Communications, 159, 15–25. DOI: https://doi.org/10.1016/j.comcom.2020.05.019 。用于中继位置与双段可靠链路联合考虑的理论背景，不替代题面链路口径。

双引擎检索脚本已运行，但 OpenAlex 在本次检索连续返回 HTTP 429；AnySearch 候选仅用于发现，未作为双源交叉验证。上述条目均以 DOI 出版页或标准机构页面单独核验。

## 14 建模阶段完成门槛

本报告与 `术语表格.md` 完成作者自检后，必须由未参与编写的独立质检 Subagent 执行 M1。M1 未通过前，不进入 P0/P1 编程阶段，不生成正式数值结论。
