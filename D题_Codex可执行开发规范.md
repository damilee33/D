# D题无人机运输与通信协同优化 Codex 可执行开发规范

## 0. 文档定位

本文档是后续 Codex 实现 2026 年中国研究生数学建模竞赛 D 题《山区洪涝灾害下无人机运输与通信协同优化》的唯一开发入口。它把已经确认的“公共物理计算层 → Q1 单点运输 → Q2 多点调度 → Q3 通信中继 → Q4 任务分区”路线转换为可执行的软件合同。

本文档只规定后续实现，不包含任何求解代码，也不代表已经得到数值结果。

规范中的关键字含义如下：

- **必须**：题目硬约束、附件规则或已确认且不得擅改的建模路线。
- **应当**：默认实现要求；若偏离，必须写明原因和验证证据。
- **禁止**：不得实施的行为；触发时必须停止而不是静默绕过。
- **待配置**：只能从题目附件读取或由用户确认，不能由 Codex 猜测。

## 1. 来源优先级与冲突处理

后续实现必须遵守以下来源优先级：

1. D 题题目正文、附录 1—3及原始数据附件；
2. 用户后续明确确认的模型或参数修改；
3. 本开发规范中已经冻结的模型路线；
4. Codex 的工程实现建议。

若本规范中的字段名、数量、单位或规则与原始附件不一致，必须以原始附件为准，同时停止当前阶段，生成差异报告并请求确认。不得静默修改输入、补造数值或继续运行。

题目和数据附件必须只读。首次运行必须保存全部输入文件的相对路径、文件大小和 SHA-256；后续运行若哈希变化，必须重新执行数据审计和全部受影响阶段。

## 2. 项目目标

项目的最终目标是构建一个可复现、可审计、分层求解的无人机救援调度系统，依次完成：

1. 统一计算节点间距离、DEM 地形净空、飞行时间、运输/中继能耗、SOC、充电时间和通信链路可用性；
2. 求解 Q1 的服务区—机型最大安全载荷矩阵和单服务区货箱组批方案；
3. 求解 Q2 的异构运输无人机多点、多架次、无人机—电池联合调度方案；
4. 在 Q2 基础上求解 Q3 的运输—中继联合调度和连续通信保障方案；
5. 固定 Q3 任务关系，求解 Q4 的 2 组和 3 组任务分区及独立资源配置；
6. 同时生成 baseline、主模型、约束审计、Pareto 比较和可复现记录。

最终实现必须能够回答题目要求的运输路线、架次时序、逐箱送达、资源占用、通信保障、任务分区和资源缺口，不得只输出一个综合目标值。

## 3. 已冻结的总体建模路线

总体路线固定为：

```text
原始附件
  ↓
数据审计与统一对象编号
  ↓
公共物理计算层
  ↓
Q1 最大安全载荷 + 多约束 Bin Packing + 局部搜索
  ↓
Q2 EDF 可行初解 + 多车型多仓次 VRP + ALNS + 电池调度 + Pareto
  ↓
Q3 Q2 解 + 通信轨迹离散化 + DEM/LOS 覆盖矩阵
   + 候选中继点 + 时空集合覆盖 + 中继调度 + 局部联合调整
  ↓
Q4 Q3 任务关联图 + 约束图划分 + 独立资源核算
  ↓
统一验收与结果导出
```

Q1—Q4 不是四套互相独立的程序。公共物理量只能由公共模块计算；后续问题必须读取上一问题的冻结结果，不得重新使用另一套口径计算同一物理量。

## 4. 禁止事项

后续 Codex 实施时禁止：

1. 把 Q1—Q4 合并为一个超级 MILP；
2. 用一个遗传算法同时搜索货箱、路径、无人机、电池、中继经纬度、高度和能源组件；
3. 让 Q1—Q4 各自重复实现距离、DEM、时间、能耗、SOC 或通信公式；
4. 擅自改变“Q3 继承 Q2、Q4 固定 Q3”的关系；
5. 使用随意加权和并拍脑袋指定权重；
6. 把医疗物资期望时间或首批截止时间降为软约束；
7. 拆分货箱、重复配送货箱、跨服务区执行 Q1 组批；
8. 允许不同机型共享电池，或允许同一能源资源同时充电和执行任务；
9. 把附件中的节点海拔与 DEM 栅格高程混为同一概念；
10. 忽略每次服务区投送后从服务区上方 30 m 作业高度重新爬升的规则；
11. 允许中继无人机之间多跳，或仅检查中继两段链路中的一段；
12. 只在少数轨迹点检查通信、却声称全过程连续通信；
13. 在 Q4 中拆开同一运输架次访问的服务区；
14. 将预测、启发式解或聊天摘要当作题目真值；
15. 未经真实运行就填写结果数值、图表或“优于 baseline”的结论；
16. 为了让程序跑通而静默放宽硬约束；
17. 修改原始附件或覆盖已有用户文件；
18. 在完成当前阶段验收前提前进入下一阶段。

## 5. 输入数据合同与开工门禁

### 5.1 必需输入

实现前必须定位并只读加载：

- `调度中心与服务区.xlsx`：O01、15 个服务区、坐标、地面海拔、人口等；
- `物资需求与配送时限.xlsx`：服务区汇总需求、80 个逐箱货箱、物资类型、重量、体积、期望时间、首批标记与截止时间；
- `运输无人机数据.xlsx`：A/B/C 三类机型、8 架实体运输无人机、分机型共享电池及飞行/能耗/充电参数；
- `中继无人机数据.xlsx`：中继机、能源组件、悬停上限、准备/建链/周转/充电参数；
- `通信链路参数.xlsx`：G01、运输机、中继机的双向收发参数、频率、接收灵敏度、衰落裕量、系统损耗和遮挡附加损耗；
- 30 m DEM 及其坐标参考、NoData、范围和分辨率信息；
- 地理空间数据说明文件；道路、水系、水体和村镇数据仅用于场景核验与可视化，除非题目明确把它们作为约束，否则不得擅自加入路径可行性条件；
- 题目提供的结果模板和检查说明。

### 5.2 已知规模校验

下列值作为开工时的审计断言，而不是散落在代码中的硬编码常量：

- 1 个临时调度中心 O01，15 个服务区；
- 80 个不可拆货箱；
- 3 类运输机，8 架实体运输机，构成为 4A、2B、2C；
- 共享运输电池数量为 A 类 6、B 类 4、C 类 4；
- 2 架中继无人机，6 组可更换能源组件；
- DEM 标称空间分辨率为 30 m；
- 历史附件审计得到总货物质量 758 kg、总体积 2.011 m³、首批保障货箱 30 箱；医疗 16 箱、饮用水 36 箱、食品 19 箱、生活卫生用品 9 箱。

如果实际附件与任一断言不一致，程序必须输出 `data_audit_mismatch.json` 并停止。原始附件优先，不得为了通过断言修改数据。

### 5.3 数据质量检查

数据审计至少检查：

- 对象 ID 唯一且引用完整；
- 坐标格式、坐标系、经纬度顺序和空间范围一致；
- O01 和全部服务区在 DEM 有效覆盖范围内；
- 节点海拔与邻近 DEM 高程差异被记录但不互相覆盖；
- 汇总需求与逐箱货箱在箱数、重量、体积、首批标记和服务区归属上逐项一致；
- 所有单位可转换到统一内部单位；
- 非首批货箱的首批截止时间为空属于结构性空值，不插补；
- 物理参数非负、速度/能量/容量大于零、SOC 范围合法；
- 不同机型电池映射完整且不可交叉；
- DEM NoData、边界和采样策略已记录；
- 结果模板的字段、顺序和数据类型已登记。

开工门禁的输出为 `results/00_audit/data_audit.json`、`input_manifest.json` 和一份人可读摘要。数据审计未通过时，禁止进入公共物理层。

## 6. 单位、坐标与对象标准

内部计算统一使用：

- 距离：m；
- 高度/海拔：m；
- 时间：s，导出时可附分钟或时间戳；
- 质量：kg；
- 体积：m³；
- 能量：kWh；
- 功率：kW 或 W，但必须在接口层统一；
- SOC：0—1；导出可显示为百分数；
- 频率：按链路公式需要显式转换为 MHz；
- 三维通信距离：km 后代入 FSPL 公式；
- 功率和链路预算：dBm/dB，禁止与线性功率混算。

必须区分：

- 节点地面海拔；
- 服务区作业海拔 = 服务区地面海拔 + 30 m；
- O01 作业海拔 = O01 地面海拔；
- 航段计划巡航海拔 = 航段经过 DEM 像元的最高地面高程 + 50 m；
- 中继悬停离地高度与中继绝对飞行海拔。

所有公共函数的输入输出都必须带明确单位；禁止用变量名暗示单位却不校验。

## 7. 推荐工程结构

后续代码应按职责拆分，建议结构如下；可以调整文件名，但不得合并职责或复制公式。

```text
project/
├─ data/                         # 原始附件，只读
├─ config/
│  ├─ paths.yaml
│  ├─ solver.yaml
│  └─ scenarios.yaml
├─ src/
│  ├─ io/                       # Excel、DEM、结果模板、对象校验
│  ├─ domain/                   # Node、Cargo、Drone、Battery、Trip、RelayMission 等对象
│  ├─ common/
│  │  ├─ units.py
│  │  ├─ geometry.py
│  │  ├─ dem.py
│  │  ├─ flight.py
│  │  ├─ transport_energy.py
│  │  ├─ battery.py
│  │  ├─ relay_energy.py
│  │  ├─ communication.py
│  │  ├─ timeline.py
│  │  └─ metrics.py
│  ├─ q1/
│  ├─ q2/
│  ├─ q3/
│  ├─ q4/
│  ├─ validation/
│  └─ reporting/
├─ scripts/                     # 单一阶段入口和全流程入口
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  └─ regression/
├─ cache/                       # 可删除、可重建的预计算缓存
├─ results/
│  ├─ 00_audit/
│  ├─ 01_common/
│  ├─ q1/
│  ├─ q2/
│  ├─ q3/
│  ├─ q4/
│  └─ validation/
└─ figures/
```

模块间只通过明确的数据对象或版本化 JSON/CSV 文件传递结果。优化器不得直接读取原始 Excel 并自行解释字段。

## 8. 公共基础计算层

### 8.1 模块 A：节点、距离与 DEM 航段

输入：起点、终点、节点作业海拔、DEM 和坐标参考。

必须输出每个有向航段 `i → j` 的：

- 水平距离 `d_ij`；
- DEM 采样轨迹与最高地面高程 `Hmax_ij`；
- 计划巡航海拔 `Hcruise_ij = Hmax_ij + 50 m`；
- 爬升高度 `h_up_ij`；
- 下降高度 `h_down_ij`；
- 是否完全位于 DEM 有效范围；
- 输入哈希、采样方法和缓存版本。

两节点间运输航段必须是水平直线。DEM 采样应覆盖直线穿过的栅格，不能只取端点或稀疏等距点。正反向航段的水平距离和最高地形应一致，但爬升、下降可以因节点作业海拔不同而不同。

验收：手工选择至少一个平坦航段、一个越岭航段、一个正反向航段和一个 DEM 边界航段进行独立核对。

### 8.2 模块 B：载荷—航程—运输能耗

输入：机型、航段、当前有效载荷、附件机型参数。

必须严格实现附录 2 的等效航程、飞行时间、水平巡航能耗和爬升附加能耗公式；下降附加能耗按题目规定不单独计算。不得用外部经验公式替换题目公式。

多点架次必须按实际卸货顺序更新剩余载荷。若路线为 `O01 → S3 → S7 → O01`，三个航段的载荷分别由起飞货物、在 S3 卸货后的剩余货物和在 S7 卸货后的剩余货物确定。

输出至少包含每航段的载荷、等效航程、时间、水平能耗、爬升能耗和总能耗，并给出架次汇总。

### 8.3 模块 C：作业时间线

时间线必须显式包含附件规定的准备、装载、爬升、巡航、下降、交接、建链、悬停服务和周转时间；某项对特定问题不适用时应为零并说明来源，不能直接遗漏。

每个货箱的送达时刻定义为该箱所属服务区完成题目规定交接/投送的时刻。Q2/Q3 必须保留逐箱送达时刻，不能只保留服务区或架次完成时刻。

### 8.4 模块 D：SOC 与两阶段充电

每一块运输电池和每一组中继能源组件都是独立资源，具有自己的占用区间、任务后 SOC、充电区间和再次可用时刻。

必须满足：

- 初始 SOC = 100%；
- 任务后 SOC 由实际能耗和该资源可用能量计算；
- SOC 不得低于对应返航安全下限；
- 再次投入任务前必须充至 100%；
- SOC 低于 90% 的 0—90% 阶段占等效完全充电时间的 65%；
- 90—100% 阶段占等效完全充电时间的 35%；
- 各阶段按 SOC 增量线性折算；
- 同一资源的任务占用与充电区间不得重叠；
- 不同资源可以并行充电；
- 同机型运输电池可在该机型实体无人机间共享，不同机型不可混用。

充电函数必须对 SOC=0、0.9、1 以及跨越 0.9 的情况做边界单元测试。

### 8.5 模块 E：通信链路

输入为同一时刻两个通信端点的三维位置及双向设备参数。

必须依次完成：

1. 沿三维视线对 DEM 采样，得到地形遮挡状态；
2. 根据收发方向分别计算最大允许总传播损耗；
3. 双向链路门限取两个方向允许损耗的较小值；
4. 按题目单位计算三维距离和自由空间传播损耗 FSPL；
5. 若遮挡，加入附件规定的遮挡附加损耗；
6. 用总传播损耗与双向门限比较，输出单链路可用性；
7. 直连可用时使用 G01；直连不可用时，只有“运输机—中继机”和“中继机—G01”同一时刻同时可用，才记为中继通信；否则为中断。

禁止中继间多跳。每架运输机任一时刻只能由 G01 或一架中继机提供通信保障。

通信轨迹离散化必须覆盖爬升、巡航、下降和投送全过程。离散步长是配置项，必须进行加密复验；若加密后发现原方案漏检中断，原方案判定失败。不得以单一粗步长直接宣称连续通信。

### 8.6 模块 F：统一指标与可行性审计

统一保留：

- `N_T`：运输架次数；
- `E_T`：运输总能耗；
- `T_transport`：运输任务完成时间；
- `D`：非硬时限物资的及时性损失；
- `N_R`：中继架次数；
- `E_R`：中继总能耗；
- `T_joint`：运输和中继全部返回 O01 的联合完成时间；
- 各硬约束违例计数，正常可行解必须全部为零。

指标函数只负责度量，不得在不同问题中重定义同名指标。

## 9. Q1 单点往返运输能力与组批

### 9.1 问题边界

每个架次严格为 `O01 → Si → O01`，只服务一个服务区；同一服务区可有多个架次；不得跨区组批。Q1 不分配实体无人机和具体电池，但必须使用机型单组电池能量与返航安全余量判断能量可行性。

### 9.2 输出

必须输出：

- 15×3 的服务区—机型最大安全载荷矩阵 `W_safe[i,k]`；
- 每个服务区的批次、货箱清单、机型、重量、体积、能耗、时间和返航 SOC；
- baseline 与主模型指标；
- 安全余量 10%、15%、20%、25%、30% 的敏感性结果；
- 每个批次的约束审计。

### 9.3 最大安全载荷

对每个服务区和机型，在重量上限、体积上限、等效航程/能耗公式与返航安全余量约束下求最大可行载荷。若可行性随载荷单调，优先使用有边界的单调搜索；若附件公式导致非单调，必须改用完整区间搜索并记录原因。

最大安全载荷是物理能力上限，不等于实际货箱组合；实际组批仍须满足货箱不可拆分和体积约束。

### 9.4 Baseline

按服务区独立处理：货箱按重量从大到小排序；对每批优先选择满足安全航程且容量足够的最小合适机型；使用 FFD 组批。baseline 必须使用同一公共物理层和同一约束审计器。

### 9.5 主模型

冻结为“最大安全载荷 + 重量/体积/能量多约束 Bin Packing + 局部搜索”。局部搜索至少允许箱间移动、箱间交换、批次合并和机型替换，但每次操作后必须完整复核约束。

在所有货箱唯一配送且硬约束满足后，按第 13 节的分层优先级比较架次数、总能耗和累计作业时间，不使用无依据的加权和。

### 9.6 Q1 验收

- 每个货箱恰好出现一次；
- 不存在跨服务区批次；
- 每批重量、体积、能量和返航余量均可行；
- 最大安全载荷在边界两侧通过“可行/不可行”复验；
- 主模型至少相对 baseline 非支配；若被 baseline 支配，Q1 不通过；
- 安全余量增大时，可行载荷不得出现无解释的系统性上升；
- 所有数值可由公共模块逐航段重算一致。

## 10. Q2 异构无人机多点多架次调度

### 10.1 继承与新增

Q2 继承公共航段、时间、能耗、货箱和 Q1 的安全载荷/组批经验，但不被 Q1 的单服务区批次锁死。Q2 允许一个架次访问一个或多个服务区，必须返回 O01。

新增实体资源：8 架运输机、分机型共享电池池、充电周转和架次开始时刻。

### 10.2 硬约束

必须同时满足：

- 货箱不可拆分、每箱恰好送达一次；
- 每个架次重量、体积、能量和返航安全余量可行；
- 路线从 O01 出发并返回 O01；
- 机型与实体无人机一致；
- 电池类型与机型一致；
- 同一无人机的占用区间不重叠；
- 同一电池的任务和充电区间不重叠；
- 电池再次执行任务前 SOC=100%；
- 医疗物资在附件期望送达时间前送达；
- 首批保障货箱在首批截止时间前送达；
- 所有服务与飞行时间按统一时间线累计。

其他物资的期望送达时间进入及时性损失，不作为擅自设定的硬截止。

### 10.3 Baseline

固定使用“Q1 组批结果 + 单服务区往返 + EDF 调度”：

1. 仍采用 `O01 → Si → O01`；
2. 按硬截止时间和期望时间生成 EDF 顺序；
3. 贪心分配最早可用且机型合适的实体无人机和已充满电的同型电池；
4. 若任何硬时限或资源约束失败，输出明确不可行原因。

### 10.4 主模型

冻结为“分层构造可行初解 + ALNS + 显式无人机/电池调度 + Pareto 评价”。

ALNS 的解必须同时编码或可完整解码为：货箱分组、服务区访问顺序、机型、具体实体无人机、具体电池和开始时刻。

破坏/修复算子至少覆盖：

- 服务区移除与重插；
- 最迟/高延误货箱移除与重插；
- 高能耗架次移除与重组；
- 相关服务区成组移除；
- 架次拆分/合并；
- 机型替换；
- 无人机/电池重新排程。

任何产生不可行解的算子必须执行修复或拒绝，不得靠大罚值长期保留明显硬约束违例。随机搜索必须固定并记录种子；至少进行多种子重复并报告稳定性。

### 10.5 输出

每个运输架次必须包含：

- 架次 ID、机型、实体无人机 ID、电池 ID；
- 装载货箱 ID；
- 路线与逐段开始/结束时刻；
- 各服务区到达和交接时刻；
- 每箱实际送达时刻；
- 各航段剩余载荷、能耗；
- 出发 SOC、返航 SOC、后续充电区间；
- 准备、装载、飞行、交接和周转区间。

同时输出无人机甘特数据、电池 SOC/占用数据、服务区送达摘要、baseline 对比和 Pareto 解集。

### 10.6 Q2 验收

- 全部硬约束违例为零；
- 逐箱、逐架次、逐无人机和逐电池四种视角可相互对账；
- `T_transport` 等于最后一架运输机完成最后架次并返回 O01 的最晚时刻；
- 用独立时间线重放器重算后结果一致；
- 主模型不得被 baseline 在及时性、完成时间、能耗和架次数上同时支配；
- 多种子运行给出最好值、典型值和离散程度，不能只报一次幸运结果；
- Q2 通过后冻结为版本化方案 `S2`，供 Q3 读取。

## 11. Q3 通信约束下运输—中继联合调度

### 11.1 继承原则

Q3 是 `Q2 + 通信约束`，不是另起一套运输模型。初始输入必须是已通过 Q2 验收的 `S2`。允许的联合调整仅限：在通信或中继资源不可行时，局部调整运输架次开始时间、局部路径/批次或相关资源安排；任何调整都必须重新执行 Q2 全部硬约束审计并生成新的可追溯版本。

### 11.2 通信缺口提取

对 S2 中每个运输架次的准备后飞行、爬升、巡航、下降和投送时间线生成三维轨迹，先计算与 G01 的直连状态，得到直连不可用的时空区间。直连可用区间不占用中继资源。

### 11.3 候选中继点与覆盖预计算

候选悬停点必须位于 DEM 有效范围内，高度不得超过附件规定的最大离地悬停高度。候选点生成应结合 DEM 网格、通信缺口的空间位置和必要的高度层；禁止在无界连续空间中直接让遗传算法搜索全部位置。

对每个候选点—高度组合预计算：

- 中继往返 O01 的地形净空、飞行时间、能耗和返航余量；
- 中继—G01 回传链路时序可用性；
- 对每个运输通信缺口的接入链路时序可用性；
- 两段链路同时可用的覆盖矩阵；
- 可连续覆盖的时间区间。

### 11.4 Baseline

先固定 S2，不改变运输方案。对每个直连通信缺口，按时间顺序选择最近可行候选点、最早可用中继机和最早可用能源组件。若仍不可行，再允许只调整相关运输架次开始时间。baseline 的失败必须指出是覆盖不足、两架中继并发不足、能源组件不足、充电不足还是运输时限冲突。

### 11.5 主模型

冻结为“候选点离散化 + 时空集合覆盖 + 中继无人机/能源组件调度 + 局部联合调整”。

主模型必须决定：

- 哪些缺口由哪一架中继机服务；
- 中继悬停点和飞行海拔；
- 中继架次起止与建链、服务、返航时刻；
- 能源组件 ID、任务后 SOC、充电和再次可用时刻；
- 必要时对相关运输任务做的局部调整。

一架中继在同一悬停位置和服务时段可以支持附件规则允许的多个运输任务，但每一条通信关系都必须逐时刻验证；不得仅凭几何半径判定覆盖。

### 11.6 Q3 输出

在完整保留 Q2 输出的基础上，增加：

- 每个运输任务的直连/中继状态时间线；
- 每个通信缺口的起止、所用中继和覆盖证据；
- 每个中继架次的路线、悬停点、绝对海拔、离地高度、建链和服务区间；
- 中继能源组件占用、SOC 和充电区间；
- 任意时刻两段链路的损耗、门限和可用性审计；
- baseline、主模型和 Pareto 解集；
- 运输局部调整的前后差异。

### 11.7 Q3 验收

- 所有运输机在题目要求阶段的通信中断总时长为零；
- 任一中继状态同时满足接入和回传两段链路；
- 不存在中继多跳；
- 中继悬停点在 DEM 内且离地高度不超上限；
- 中继机和能源组件的占用/充电不冲突；
- 所有局部运输调整仍满足 Q2 硬约束；
- 对通信时间步长和候选点密度做加密复验，结论稳定；
- `T_joint` 等于所有运输机和中继机完成最后架次并返回 O01 的最晚时刻；
- 主模型不被 baseline 在优先指标上支配；
- 通过后冻结联合方案 `S3`，Q4 只能读取，不能修改。

## 12. Q4 任务分区与资源配置

### 12.1 固定条件

Q4 的核心输入是冻结的 S3，而不是重新从 80 个货箱开始优化。必须保持 S3 中的货箱组批、服务区访问顺序、运输/中继任务安排及通信关系不变。

若同一运输架次访问多个服务区，这些服务区形成硬 must-link 关系，必须属于同一任务组。若一个不可拆分的中继架次按 S3 同时服务多个运输任务，则其关联服务区也必须作为同一可执行单元处理；若附件/结果模板对该情况另有明确口径，以题目口径为准并记录。

### 12.2 图构建

建立 Q3 任务关联图：

- 节点：15 个服务区；
- 硬边：同一运输架次共同访问；
- 必要的硬边：固定中继任务导致不可拆分的共同通信保障关系；
- 边权/节点属性：运输任务量、能源量、作业时间、资源并发、地理距离和通信关联，用于评价而非改变硬关系。

先压缩硬 must-link 连通分量，再分别求 K=2 和 K=3 的分区。每个服务区恰属一个组，每组至少一个服务区。

### 12.3 Baseline

在 must-link 约束可行的前提下，使用服务区地理位置做 K-means 或层次聚类。若普通 K-means 破坏 must-link，必须在压缩后的超节点上聚类，不能先聚类后强行拆改 S3。

### 12.4 主模型

冻结为“Q3 任务关联图 + 约束图划分 + 独立资源核算”。规模仅 15 个服务区，应优先采用可验证的整数规划或枚举/剪枝；无需再引入大型元启发式。

评价维度包括：

- 各组独立执行所需的 A/B/C 实体运输机数量；
- 各组独立执行所需的 A/B/C 共享电池数量；
- 各组独立执行所需的中继机和能源组件数量；
- 各类资源总配置规模；
- 相对峰值并发需求的冗余；
- 组间工作量/任务时长/能耗均衡；
- 与现有库存相比的逐类资源缺口。

资源需求不能用“该组曾用过多少个 ID”简单计数，必须根据固定任务时间线和资源兼容性求最小可行并发数；电池和能源组件还要考虑任务后充电至 100% 的周转。

### 12.5 Q4 输出与验收

分别输出 K=2 和 K=3：

- 服务区分组；
- 每组包含的运输架次和中继架次；
- 各类最小资源需求；
- 相对库存的冗余或缺口；
- 工作量与完成时间指标；
- baseline 与主模型比较；
- 固定 S3 关系审计。

验收要求：15 个服务区完整且互斥覆盖；每组非空；所有 must-link 均未被切断；资源禁止跨组调配；每组可独立重放 S3 中属于该组的任务；资源缺口按机型/资源类型逐项给出，不能只报总数。

## 13. 优先级与 Pareto 原则

### 13.1 统一分层优先级

所有问题统一采用以下层级，禁止直接写成无依据的加权和：

1. **可行性层**：安全、货箱唯一配送、硬时限、资源不冲突、返航余量、连续通信等全部硬约束为零违例；
2. **及时性层**：在可行解中优先降低其他物资的配送延误/及时性损失；
3. **效率层**：比较完成时间、总能耗和架次数的 Pareto 权衡；
4. **解释与稳健层**：在近似等价的 Pareto 解中优先选择结构简单、资源余量更稳健、对安全余量/时间步长/随机种子不敏感的方案。

### 13.2 Pareto 输出要求

不得只输出一个“综合最优值”。每个主问题至少保留：

- 一组可复核的非支配解；
- 最低完成时间方案；
- 最低能耗方案；
- 最少架次方案；
- 一份折中推荐方案及其推荐依据。

若需要从 Pareto 集选单一方案，只能采用透明、可解释的规则，例如在及时性最优集合中选择距理想点最近者；规则和归一化方法必须写入配置与报告。不得隐藏权重。

### 13.3 Baseline 判定

主模型不要求每个指标都严格优于 baseline，因为指标间存在真实权衡；但主模型不得被 baseline 同时支配。若主模型只在牺牲更高优先级指标后改善低优先级指标，则判定失败。

## 14. 数据与模型继承合同

### 14.1 公共缓存

公共层一次生成、后续只读：

- 节点标准表；
- 有向航段几何/DEM 表；
- 按机型和载荷查询的时间/能耗计算接口；
- 统一充电函数；
- 通信链路计算接口；
- 输入哈希与配置版本。

### 14.2 阶段产物

- Q1 输出 `S1`：安全载荷矩阵、单点批次和敏感性结果；
- Q2 输出 `S2`：可完整重放的运输调度；
- Q3 输出 `S3`：可完整重放的运输—中继联合调度；
- Q4 读取冻结的 `S3`，输出 K=2/K=3 分区与独立资源配置。

每个 S1/S2/S3 文件必须包含：方案版本、父版本、输入哈希、配置哈希、随机种子、求解状态、目标指标、硬约束审计摘要和生成时间。修改上游结果后，下游缓存自动失效。

### 14.3 复用矩阵

- 经纬度/水平距离：Q1—Q4 共用；
- DEM 航段最高高程、巡航高度、飞行时间、运输能耗：Q1—Q3 计算，Q4 继承；
- 载重/体积：Q1—Q3 计算，Q4 继承；
- SOC、充电、资源占用区间：Q2—Q4 共用；
- 货箱时限与逐箱送达：Q2/Q3 计算，Q4 继承；
- LOS、链路预算、中继能耗：Q3 计算，Q4 继承；
- 统一指标与可行性审计：所有问题共用。

## 15. 输出文件合同

具体列名可根据题目模板调整，但信息不得减少。建议至少生成：

```text
results/00_audit/input_manifest.json
results/00_audit/data_audit.json
results/01_common/nodes.csv
results/01_common/edges.csv
results/q1/safe_payload_matrix.csv
results/q1/baseline_batches.csv
results/q1/final_batches.csv
results/q1/safety_margin_sensitivity.csv
results/q2/baseline_schedule.json
results/q2/final_schedule.json
results/q2/trips.csv
results/q2/cargo_delivery.csv
results/q2/drone_timeline.csv
results/q2/battery_timeline.csv
results/q2/pareto.csv
results/q3/baseline_joint_schedule.json
results/q3/final_joint_schedule.json
results/q3/communication_timeline.csv
results/q3/relay_missions.csv
results/q3/relay_energy_timeline.csv
results/q3/pareto.csv
results/q4/partition_k2.json
results/q4/partition_k3.json
results/q4/resource_comparison.csv
results/validation/constraint_audit.json
results/复现清单.json
```

CSV 用于扁平明细，JSON 用于包含嵌套路线和父子关系的方案。不得只输出 pickle 等不可人工审查格式。题目指定的 XLSX 模板在最终阶段由已验收的 CSV/JSON 填充，不允许让模板成为唯一结果来源。

## 16. 测试与验收总则

### 16.1 单元测试

至少覆盖：

- 坐标和单位转换；
- DEM 穿栅格和最高高程；
- 正反向航段爬升/下降；
- 空载、满载和中间载荷能耗；
- 多点卸货后的载荷递减；
- SOC 边界和两阶段充电；
- 双向链路门限、遮挡和 FSPL 单位；
- 直连、中继、通信中断三种状态；
- 资源区间重叠检测；
- 货箱唯一性和 must-link 分区。

### 16.2 集成测试

建立一个极小、可手算的合成场景，仅用于验证程序逻辑，不用于替代真实数据结论。随后用真实附件运行最小 smoke test：一个服务区、少量货箱、单个运输架次、一个通信缺口和一个中继候选点。

### 16.3 回归测试

固定输入哈希、配置和随机种子后，关键汇总结果应可重复。公共公式或时间线修改后，必须显式更新回归基线并说明影响，禁止自动接受变化。

### 16.4 独立重放

最终验收器必须与优化器解码逻辑分离：它读取已导出的方案，从原始附件和公共物理接口重新计算全部约束与指标。优化器自报“可行”不能代替独立重放。

## 17. 执行顺序和阶段门禁

严格按以下顺序实施：

1. 定位题目、全部数据和结果模板；生成输入清单和哈希；
2. 完成数据审计，核对对象数、汇总需求、单位、坐标系和 DEM；
3. 实现并测试公共单位、几何、DEM、飞行、能耗、SOC/充电模块；
4. 实现并测试通信模块，但暂不求解 Q3；
5. 先跑 Q1 baseline，再跑 Q1 主模型和安全余量敏感性；Q1 验收通过后冻结 S1；
6. 先跑 Q2 baseline，确认 EDF、无人机和电池时间线可行；再运行 Q2 ALNS；通过后冻结 S2；
7. 用 S2 提取通信缺口，生成候选点与覆盖矩阵；先跑 Q3 baseline，再跑主模型和局部联合调整；通过后冻结 S3；
8. 从 S3 构建任务关联图，分别运行 Q4 的 K=2、K=3 baseline 和主模型；
9. 运行独立重放、全局约束审计、Pareto 比较和敏感性/稳定性检查；
10. 最后才填充题目结果模板、生成正式图表和复现清单。

任何阶段未通过门禁时，必须停在该阶段。不得以“后续会修复”为由继续生成下游结果。

## 18. 推荐实施批次

为降低返工风险，后续 Codex 应按小批次提交实现：

### 批次 P0：输入与合同

只实现数据读取、对象模型、单位转换、哈希和数据审计。验收后再继续。

### 批次 P1：公共物理层

实现 DEM、距离、飞行时间、运输/中继能耗、SOC 与充电，并用手算样例测试。

### 批次 P2：Q1

实现安全载荷、baseline、Bin Packing、局部搜索和敏感性。先形成第一个完整可复现结果闭环。

### 批次 P3：Q2 baseline 与时间线

先证明实体无人机/电池调度器和独立重放器可靠，再接入 ALNS。

### 批次 P4：Q2 ALNS

逐个加入算子，保留消融结果，输出 Pareto 集并冻结 S2。

### 批次 P5：通信预计算与 Q3 baseline

完成轨迹、LOS、链路预算、候选点和覆盖矩阵；先解决固定 S2 的通信缺口。

### 批次 P6：Q3 主模型

加入时空覆盖、中继/能源调度和受控的局部联合调整，冻结 S3。

### 批次 P7：Q4 与总验收

实现 must-link 图划分、资源并发核算、K=2/K=3 比较及全流程独立重放。

## 19. 失败条件与回退策略

- 数据或单位不明确：停止，列出字段、候选解释及影响，不猜测；
- Q1 最大安全载荷非单调：放弃二分，改用区间扫描/全量可行性搜索；
- Q2 ALNS 长时间找不到可行解：回退到 EDF baseline，从可行解逐步开放多点合并，不放宽硬约束；
- Q2 结果不优于 baseline：检查算子、解码器和目标层级，不能用改权重掩盖；
- Q3 固定 S2 无法覆盖：先做开始时间局部调整，再做相关架次局部重排；仍不可行时报告资源/地形瓶颈；
- Q3 计算量过大：增加缓存、分层筛选候选点和自适应加密，不删除 LOS 或双向链路检查；
- Q4 K=3 因 must-link 分量不足而不可行：明确报告结构性不可行，不拆硬连接；
- 求解器不可用：可更换实现工具，但不得更换已确认模型或降低约束；
- 任一输出无法被独立重放：该阶段判定失败。

## 20. 可复现与日志要求

每次正式运行必须记录：

- 输入文件 SHA-256；
- 源码版本或提交号；
- Python、主要依赖和求解器版本；
- 操作系统与运行时间；
- 所有配置项和随机种子；
- 各阶段开始/结束时间、迭代数、停止条件；
- baseline 和主模型指标；
- 硬约束审计结果；
- 唯一复现命令。

搜索过程日志不得只写“成功/失败”，应记录当前可行解、Pareto 档案大小、被拒绝操作类型和不可行原因统计，以便诊断。

## 21. 最终完成定义

只有同时满足以下条件，后续 Codex 才能声明实现完成：

- 原始附件已全部定位、哈希并通过数据审计；
- 公共计算层只有一套实现，单位和公式与题目附录一致；
- Q1—Q4 的 baseline 和主模型均已真实运行；
- S1、S2、S3 的继承链可追溯，Q4 未修改 S3；
- 所有硬约束违例为零；
- 运输、通信和资源时间线可被独立重放；
- Pareto 与优先级规则已按本规范执行，没有无依据权重；
- 安全余量、时间离散、候选点密度和随机种子稳定性已检查；
- 结果模板、CSV/JSON、图表和复现清单相互一致；
- 未编造数值，未修改原始附件，未隐瞒不可行或未验证部分。

在上述完成定义之前，只能报告“已完成到某阶段”，不得报告“整题已完成”。
